spring:
  datasource:
    url: jdbc:postgresql://localhost:54326/email_db
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    url: jdbc:postgresql://localhost:54326/email_db
    user: test
    password: test
    driver-class-name: org.postgresql.Driver